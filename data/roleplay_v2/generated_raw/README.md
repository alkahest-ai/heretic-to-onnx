# generated_raw

This folder contains freshly generated `roleplay_v2` conversation batches.

These files are not training-ready.

Next step:

```bash
python3 scripts/jsonl_to_review_table.py --input <raw-batch> --output <review-table>
```
