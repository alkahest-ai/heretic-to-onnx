# archive

This folder records prototype material that is intentionally not part of the `roleplay_v2` training backbone.

`roleplay_v1/generated/batch-0002.jsonl` is treated as an archived prototype:

- useful for sampling and salvage
- not the canonical corpus
- not part of future `*-rp` training by default
